// import 'style/common.styl'

// // Pre-concatenate some abstractions components
// // import 'components/dom/Button/Button.styl'
// // import 'components/dom/BigText/BigText.styl'

// // import addBowserClasses from 'utils/bowserToClasses'
// // import handleResize from 'utils/handleResize'
// // import prng from 'utils/prng'
// // import handleCursor from 'utils/handleCursor'

// import { h, render } from '@internet/dom'
// import App from 'components/dom/App/App'
// // import ErrorApp from 'components/dom/App/ErrorApp'
// import store from 'state/store'
// // import analytics from 'controllers/ga'
// // import Cookies from 'js-cookie'
// // import isWebGLSupported from 'utils/isWebGLSupported'

// // Do not use grpd in Arcade mode
// if (!store.arcade.get()) store.grpd.set(!!Cookies.get('grpd.allowed'))
// analytics.setup()

// prng.setSeed(+new Date())
// addBowserClasses()
// handleResize()
// handleCursor()

// analytics.pageview('Main', window.location.pathname)

// // if (isWebGLSupported()) render(<App base={document.body.querySelector('.app')} />)
// // else render(<ErrorApp base={document.body.querySelector('.app')} />)
// // We use autoDetectRenderer - stop throwing if webgl isnt enabled
// render(<App base={document.body.querySelector('.app')} />)
console.log('Lauched');
